import React from 'react';
import { MediaContainer, } from './ButtonSectionStyled';

interface TitlePageProps {

}

const TitlePage: React.FC<TitlePageProps> = () => {
  return (
    <MediaContainer >
     
    </MediaContainer>
  );
};

export default TitlePage;
